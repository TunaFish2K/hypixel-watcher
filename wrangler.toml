name = "hypixel-watcher-api"
main = "server/worker.ts"
compatibility_date = "2025-01-01"

# Workers 配置
workers_dev = true

# 如果需要自定义域名，取消下面的注释
# routes = [
#   { pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]

# CORS 配置（如果需要）
[env.production]
